package com.miui.home.launcher;

/** Fake of the CN launcher's MIUI-minus-screen compat (test shape only).
 *  Constructor intentionally package-private like the real build. */
public class LauncherAssistantCompatMIUI extends LauncherAssistantCompat {
    LauncherAssistantCompatMIUI(BaseLauncher launcher, String packageName) {
        super(launcher, packageName);
    }
}
