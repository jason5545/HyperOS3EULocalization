package com.miui.home.launcher;

/** Fake of the CN launcher's DeviceConfig (test shape only). */
public final class DeviceConfig {
    public static boolean p19LowMem;

    public static boolean isP19LowMemDevice() { return p19LowMem; }
}
