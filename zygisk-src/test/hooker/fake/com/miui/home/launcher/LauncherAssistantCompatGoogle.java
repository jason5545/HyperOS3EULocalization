package com.miui.home.launcher;

/** Fake of the CN launcher's Google-minus-screen compat (test shape only). */
public class LauncherAssistantCompatGoogle extends LauncherAssistantCompat {
    LauncherAssistantCompatGoogle(BaseLauncher launcher, String packageName) {
        super(launcher, packageName);
    }
}
